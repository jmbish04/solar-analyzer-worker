{
	"name": "solar-analyzer-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "npm run build:frontend && tsc",
		"build:frontend": "cd frontend && npm run build",
		"build:backend": "tsc",
		"deploy": "npm run build && wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"db:generate": "drizzle-kit generate",
		"db:migrate": "drizzle-kit generate && wrangler d1 migrations apply DB --remote"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"drizzle-kit": "^0.30.0",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.21.0"
	},
	"dependencies": {
		"@hono/zod-openapi": "^0.18.0",
		"@scalar/hono-api-reference": "^0.5.0",
		"drizzle-orm": "^0.38.0",
		"hono": "^4.11.4",
		"jose": "^6.0.12",
		"openai": "^4.73.0"
	}
}
